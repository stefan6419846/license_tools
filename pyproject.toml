[build-system]
requires = [
    'setuptools',
    'wheel',
]

[tool.codespell]
check-hidden = true
skip = './.git*,./.mypy_cache/*,./todo/*'

[tool.coverage.run]
branch = true
source = [
    'license_tools',
]
